applications:
  {{ charm_name }}:
    charm: "{{ CHARM_LOCATION }}/{{ charm_name }}.charm"
    num_units: 0
  juju-local:
    options:
      juju-channel: {{ TEST_JUJU_CHANNEL }}

relations:
  - - "ubuntu"
    - {{ charm_name }}
  - - "prometheus"
    - {{ charm_name }}
